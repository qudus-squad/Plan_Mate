package org.qudus.squad.data.csv.parser

enum class ProjectCsvColumnIndex(val index: Int) {
    //Change to constants
    ID(0),
    TITLE(1),
    DESCRIPTION(2),
    CREATOR_USER_ID(3),
    CREATED_AT(4),
    LAST_UPDATED_AT(5),
    STATE(6)
}