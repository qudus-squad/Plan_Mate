package org.qudus.squad.data.csv.parser

enum class LogEntryCsvColumnIndex(val index: Int) {
    USERNAME(0),
    TARGET_ID(1),
    TARGET_TYPE(2),
    ACTION(3),
    OLD_VALUE(4),
    NEW_VALUE(5),
    LOGGED_AT(6)
}

